{
  "permissions": {
    "deny": [
      "rm -rf /",
      "rm -rf ~",
      "rm -rf /*",
      "sudo rm",
      "git push --force",
      "git push -f",
      "git reset --hard",
      "DROP DATABASE",
      "DROP TABLE",
      "TRUNCATE",
      "shutdown",
      "reboot",
      "mkfs",
      "dd if="
    ]
  }
}
